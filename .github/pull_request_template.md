## What
-

## How to test
- `docker compose up --build`
- `GET /health`
- `GET /ready`

## Checklist
- [ ] `npm run lint` (if configured)
- [ ] `npm run test` (if configured)
- [ ] No secrets committed
